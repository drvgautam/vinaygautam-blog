---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title="DatumInt Academy — Semantic Data Engineering Training"
  description="Minimalist, practical training in Semantic Data Engineering, FAIR data pipelines, and GenAI with context."
>

  <!-- HERO SUBDOMAIN LANDING -->
  <section class="section">
    <div class="wrap grid-2" style="align-items:flex-start; row-gap:2rem;">
      <div
        class="card"
        style="
          background-color: var(--bg-card);
          border-radius: var(--radius-xl);
          border: 1px solid rgba(243,231,217,0.3);
          box-shadow: 0 24px 40px rgba(0,0,0,0.6);
        "
      >
        <div style="font-size:.8rem; color:var(--text-muted); margin-bottom:.5rem; display:flex; align-items:center; gap:.5rem; flex-wrap:wrap;">
          <span
            class="badge"
            style="font-size:.7rem;line-height:1.2;"
          >
            Focused Training
          </span>

          <span
            class="badge yellow"
            style="font-size:.7rem;line-height:1.2;font-weight:600;color:#2a211b;"
          >
            Minimalist
          </span>
        </div>

        <h1 style="font-size:clamp(1.6rem,2vw,2rem);line-height:1.2;color:var(--text-main);margin:0 0 .75rem;">
          The fast-track for Semantic Data Engineering skills
        </h1>

        <p style="font-size:.9rem;color:var(--text-dim);line-height:1.5;max-width:55ch;margin:0 0 1rem;">
          Learn how to build semantic data platforms, FAIR pipelines, and GenAI with context — without drowning in vendor noise or 200-hour “courses”. You’ll learn through focused, repeatable workflows actually used in delivery work.
        </p>

        <div style="display:flex;flex-wrap:wrap;gap:.75rem;align-items:center;">
          <a
            href="mailto:contact@datumint.no?subject=Academy%20Access"
            class="btn-solid"
            style="text-decoration:none;"
          >
            Request early access
          </a>

          <div style="font-size:.7rem;color:var(--text-muted);line-height:1.4;">
            Small-group format. Hands-on patterns. No fluff.
          </div>
        </div>

        <div style="margin-top:1.5rem;display:flex;flex-wrap:wrap;gap:.5rem;font-size:.7rem;color:var(--text-muted);line-height:1.4;">
          <span class="badge">Ontology design</span>
          <span class="badge">Knowledge graphs</span>
          <span class="badge">FAIR metadata</span>
          <span class="badge">Lineage & observability</span>
          <span class="badge">GenAI with citations</span>
          <span class="badge">Kubernetes delivery</span>
        </div>
      </div>

      <!-- Right-hand card: newsletter / “stay in the loop” -->
      <div
        class="card"
        style="
          background-color: var(--bg-card);
          border-radius: var(--radius-xl);
          border: 1px solid rgba(243,231,217,0.3);
          box-shadow: 0 24px 40px rgba(0,0,0,0.6);
        "
      >
        <p style="font-size:.8rem;color:var(--text-dim);line-height:1.4; margin:0 0 .75rem;">
          Want updates when we open the next cohort or publish new material?
        </p>

        <!-- reuse your newsletter component later if you want -->
        <form
          action="https://buttondown.com/api/emails/embed-subscribe/datumint"
          method="post"
          target="popupwindow"
          onsubmit="window.open('https://buttondown.email/datumint', 'popupwindow')"
          style="display:flex;flex-direction:column;gap:.75rem;"
        >
          <input
            type="email"
            name="email"
            required
            placeholder="you@company.com"
            style="
              flex:1;
              min-width:200px;
              background:rgba(243,231,217,0.07);
              border:1px solid rgba(243,231,217,0.4);
              border-radius:12px;
              padding:.6rem .75rem;
              color:var(--text-main);
              font-size:.9rem;
            "
          />
          <button
            type="submit"
            style="
              background-color: var(--accent-yellow);
              color:#2a211b;
              font-weight:600;
              border-radius:999px;
              padding:.6rem .9rem;
              font-size:.9rem;
              border:0;
              cursor:pointer;
              line-height:1.2;
              text-align:center;
            "
          >
            Subscribe
          </button>
        </form>

        <div style="margin-top:1rem;font-size:.7rem;color:var(--text-muted);line-height:1.4;">
          Occasional emails. Pure signal. Unsubscribe anytime.
        </div>
      </div>
    </div>
  </section>

  <!-- 3-STEP STRIP (Find your path / Start learning / Transform impact) -->
  <section class="section-alt">
    <div class="wrap" style="text-align:center;max-width:900px;">
      <div
        style="
          font-size:.7rem;
          font-weight:600;
          color:var(--text-muted);
          margin-bottom:1rem;
        "
      >
        How it works
      </div>

      <h2 style="font-size:1.4rem;line-height:1.3;color:var(--text-main);margin:0 0 2rem;">
        We teach what companies actually need built — not buzzwords.
      </h2>

      <div
        style="
          display:grid;
          grid-template-columns:1fr;
          gap:1rem;
        "
      >
        <div class="card" style="text-align:left;">
          <div style="font-size:.8rem;font-weight:600;color:var(--accent-yellow);margin-bottom:.5rem;">
            1 · Build semantic foundations
          </div>
          <div style="font-size:.9rem;color:var(--text-dim);line-height:1.5;">
            Model real business objects: assets, processes, measurements, regulatory constraints. Produce an ontology + graph that AI can reason about.
          </div>
        </div>

        <div class="card" style="text-align:left;">
          <div style="font-size:.8rem;font-weight:600;color:var(--accent-teal);margin-bottom:.5rem;">
            2 · Operationalise FAIR pipelines
          </div>
          <div style="font-size:.9rem;color:var(--text-dim);line-height:1.5;">
            Build ingestion and curation flows with lineage, metadata capture, and quality checks. Make your data Findable, Accessible, Interoperable, Reusable.
          </div>
        </div>

        <div class="card" style="text-align:left;">
          <div style="font-size:.8rem;font-weight:600;color:var(--accent-purple);margin-bottom:.5rem;">
            3 · Deliver GenAI with context
          </div>
          <div style="font-size:.9rem;color:var(--text-dim);line-height:1.5;">
            Attach an internal assistant that answers with citations and governance instead of hallucinating. Something you can actually show to management.
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- INSTRUCTOR / TRUST BLOCK -->
  <section class="section">
    <div class="wrap grid-2" style="align-items:flex-start; row-gap:2rem;">
      <div
        class="card"
        style="
          background-color: var(--bg-card);
          border-radius: var(--radius-xl);
          border: 1px solid rgba(243,231,217,0.3);
          box-shadow: 0 24px 40px rgba(0,0,0,0.6);
        "
      >
        <!-- profile picture -->
        <div style="margin-bottom:1rem;">
          <img
            src="/images/profile.png"
            alt="Vinay Gautam"
            width="240"
            height="240"
            loading="eager"
            decoding="async"
            style="
              width:100%;
              max-width:240px;
              height:auto;
              aspect-ratio:1;
              object-fit:cover;
              border-radius:var(--radius-xl);
              border:1px solid rgba(243,231,217,0.3);
            "
          />
        </div>

        <h2 style="margin:0 0 .75rem;font-size:1.2rem;line-height:1.3;color:var(--text-main);">
          Hi, I'm Vinay Gautam, PhD.
        </h2>

        <p style="font-size:.9rem;color:var(--text-dim);line-height:1.5;">
          I work with organisations that sit in complex, high-stakes domains
          (materials science, industrial processes, manufacturing)
          and need semantic data infrastructure that can be trusted by both AI and auditors.
        </p>

        <p style="font-size:.9rem;color:var(--text-dim);line-height:1.5;">
          My focus: GenAI with context, RAG/GraphRAG, knowledge graphs, FAIR data management, and delivering
          those systems both as MVP for startups or a more production grade, scalable product with CI/CD, observability, and handover
          — so the customer actually owns it.
        </p>

        <div style="margin-top:1rem;display:flex;flex-wrap:wrap;gap:1rem;font-size:.8rem;color:var(--text-muted);">
          <div>
            <div style="font-weight:600;color:var(--text-main);">10+ yrs</div>
            <div style="font-size:.7rem;">in modeling/simulation/advanced data / AI work</div>
          </div>
          <div>
            <div style="font-weight:600;color:var(--text-main);">EU projects</div>
            <div style="font-size:.7rem;">materials & industry</div>
          </div>
          <div>
            <div style="font-weight:600;color:var(--text-main);">Hands-on</div>
            <div style="font-size:.7rem;">we ship, not just advise</div>
          </div>
        </div>
      </div>

      <!-- right column: positioning -->
      <div style="max-width:600px;">
        <div
          style="
            font-size:.7rem;
            font-weight:600;
            color:var(--text-muted);
            margin-bottom:.5rem;
          "
        >
          Who this is for
        </div>

        <h2 style="font-size:1.4rem;line-height:1.3;color:var(--text-main);margin:0 0 1rem;">
          You want to stop just “doing dashboards” and actually
          build semantic, auditable AI foundations.
        </h2>

        <p style="font-size:.9rem;color:var(--text-dim);line-height:1.5;">
          You’ll fit perfectly if you’re:
        </p>

        <ul style="font-size:.9rem;color:var(--text-dim);line-height:1.5;padding-left:1rem;">
          <li>an internal data/AI lead who’s responsible for trust & compliance</li>
          <li>a platform/data engineer expected to “add AI” safely</li>
          <li>a researcher or specialist who knows the domain but needs the pipeline</li>
        </ul>

        <div style="margin-top:1.5rem;">
          <a
            href="mailto:contact@datumint.no?subject=Academy%20Access"
            class="btn-solid"
            style="text-decoration:none;"
          >
            Request early access
          </a>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>
